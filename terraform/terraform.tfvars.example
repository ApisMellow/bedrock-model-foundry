aws_region   = "us-east-1"
project_name = "bedrock-model-foundry"

# Optional: provide a pre-created CloudWatch log group ARN after the account-level
# API Gateway CloudWatch role is configured by an adoption stack.
# api_gateway_access_log_destination_arn = "arn:aws:logs:us-east-1:123456789012:log-group:/demo/api-access"

# The checked-in defaults define the reviewed model and two demo endpoints.
# Override the models or endpoints maps here only as a reviewed configuration change.

tags = {
  Environment = "demo"
  Owner       = "apis-mellow"
}
